box: algas/wercker-mybox
dummy:
  steps:
    - script:
        name: dummy
        code: |
          echo 'hello'

python-version:
  steps:
    - script:
        name: python version
        code: |
          python --version

dev:
  steps:
    - pip-install
    - internal/watch:
        code: python app.py
        reload: true
