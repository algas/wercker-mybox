box: algas/wercker-mybox

services:
  -id: mysql
  username: foo
  password: bar
  env:
    MYSQL_ROOT_PASSWORD: secret

dummy:
  steps:
    - script:
        name: dummy
        code: |
          echo 'hello'

build:
  steps:
    - script:
        name: migrate database
        code: |
          ./scripts/migrate.sh

migrate:
  steps:
    - pip-install
    - script:
        name: migrate database
        code: |
          ./scripts/migrate.sh

dev:
  steps:
    - script:
        name: migrate database
        code: |
          ./scripts/migrate.sh
    - internal/watch:
        code: python app.py
        reload: true
