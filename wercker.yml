box: algas/wercker-mybox

dummy:
  steps:
    - script:
        name: dummy
        code: |
          echo 'hello'

build:
  services:
    -id: mysql
    username: foo
    password: bar
    env:
      MYSQL_ROOT_PASSWORD: secret
  steps:
    - script:
        name: migrate database
        code: |
          ./scripts/migrate.sh

migrate:
  steps:
    - pip-install
    - script:
        name: migrate database
        code: |
          ./scripts/migrate.sh

dev:
  steps:
    - pip-install
    - script:
        name: migrate database
        code: |
          ./scripts/migrate.sh
    - internal/watch:
        code: python app.py
        reload: true
